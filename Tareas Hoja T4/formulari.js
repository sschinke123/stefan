
function registrar() {
  // Überprüfen, ob das Kontrollkästchen aktiviert wurde
  const checkbox = document.getElementById('inputConditions');
  
  if (!checkbox.checked) {
    alert("Bitte akzeptieren Sie die Bedingungen, um fortzufahren.");
    return; // Verhindert das Absenden des Formulars
  }
  
  // Wenn das Kontrollkästchen aktiviert ist, kann der Formularprozess fortgesetzt werden
  document.getElementById('formulario').submit();function registrar() {
  // Überprüfen, ob das Kontrollkästchen aktiviert wurde
  const checkbox = document.getElementById('inputConditions');
  
  if (!checkbox.checked) {
    alert("Bitte akzeptieren Sie die Bedingungen, um fortzufahren.");
    return; // Verhindert das Absenden des Formulars
  }
  
  // Wenn das Kontrollkästchen aktiviert ist, kann der Formularprozess fortgesetzt werden
  document.getElementById('formulario').submit();
